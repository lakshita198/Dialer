<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient">

    <!-- Number Display -->
    <TextView
        android:id="@+id/number_display"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:gravity="center"
        android:textSize="32sp"
        android:textColor="#333333"
        android:background="@drawable/number_display_bg"
        android:text=""
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="120dp"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"/>

    <!-- Grid for Buttons -->
    <GridLayout
        android:id="@+id/dialer_grid"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:columnCount="3"
        android:rowCount="4"
        app:layout_constraintTop_toBottomOf="@id/number_display"
        app:layout_constraintBottom_toTopOf="@id/bottom_controls"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <Button android:id="@+id/button1" style="@style/DialerButton" android:text="1"/>
        <Button android:id="@+id/button2" style="@style/DialerButton" android:text="2"/>
        <Button android:id="@+id/button3" style="@style/DialerButton" android:text="3"/>
        <Button android:id="@+id/button4" style="@style/DialerButton" android:text="4"/>
        <Button android:id="@+id/button5" style="@style/DialerButton" android:text="5"/>
        <Button android:id="@+id/button6" style="@style/DialerButton" android:text="6"/>
        <Button android:id="@+id/button7" style="@style/DialerButton" android:text="7"/>
        <Button android:id="@+id/button8" style="@style/DialerButton" android:text="8"/>
        <Button android:id="@+id/button9" style="@style/DialerButton" android:text="9"/>
        <Button android:id="@+id/buttonStar" style="@style/DialerButton" android:text="*"/>
        <Button android:id="@+id/button0" style="@style/DialerButton" android:text="0"/>
        <Button android:id="@+id/buttonHash" style="@style/DialerButton" android:text="#"/>
    </GridLayout>

    <!-- Bottom Controls -->
    <LinearLayout
        android:id="@+id/bottom_controls"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        app:layout_constraintBottom_toTopOf="@id/developed_by"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="16dp">

        <ImageButton
            android:id="@+id/call_button"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:background="@drawable/circle_button"
            android:backgroundTint="#4CAF50"
            android:src="@android:drawable/ic_menu_call"
            tools:ignore="SpeakableTextPresentCheck" />

        <ImageButton
            android:id="@+id/backspace_button"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_marginStart="40dp"
            android:background="@drawable/circle_button"
            android:backgroundTint="#fff"
            android:src="@drawable/ic_backspace"
            tools:ignore="SpeakableTextPresentCheck" />
    </LinearLayout>

    <!-- Developed By Text -->
    <TextView
        android:id="@+id/developed_by"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Developed by Lakshita Rakhecha"
        android:textColor="#000000"
        android:textSize="14sp"
        android:textStyle="bold"
        android:layout_marginBottom="60dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
